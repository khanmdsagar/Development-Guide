1. sudo docker run centos
2. sudo docker run –it centos /bin/bash 
3. docker exec -it container_name bash
4. docker stop container_name
5. docker rmi image_id

=> Ubuntu installation
1. apt install docker.io
2. snap install docker
3. running server => docker run -d -p 80:80 ngnix